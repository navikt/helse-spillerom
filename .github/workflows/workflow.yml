name: workflow
on:
  workflow_dispatch:
  push:

jobs:
  next-app:
    uses: ./.github/workflows/next-app.yaml
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      app: spillerom
      base-path: /
